#include<bits/stdc++.h>
#define ll long long 
#define pb push_back
#define Bl_dem ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
using namespace std;
int main()
{
Bl_dem
        int n , q;
        cin>> n >> q;
        map<int, set<int>> mp;
            set<ll > se;
            while(q--)
            {
                int x , y;
                cin>> x >> y;
                if(x>)
            }
return 0;
}